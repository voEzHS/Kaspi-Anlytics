"""AI report generation via Anthropic Claude API."""
import asyncio
import os
from typing import Optional

import anthropic
from fastapi import APIRouter, Depends, HTTPException, Query
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.database import get_db
from app.models.models import AppSettings, DeptEnum, KaspiRow
from app.analytics import engine
from app.routers.uploads import require_admin
from sqlalchemy import select

router = APIRouter(prefix="/api/v1/ai", tags=["ai"])

DEPT_LABELS = {
    "freezers": "Морозильники (Лари и Бонеты)",
    "refrigerated": "Холодильные витрины",
}

_MANDATORY_BRANDS: set[str] = {"AOLIEGE", "FRIGGIER", "LEADBROS", "XINGX", "MUXXED"}

# Deep persona and company knowledge injected as system prompt
SYSTEM_PROMPT = """Ты — Азамат, старший аналитик и стратег маркетплейса в компании TorgStore (Казахстан).
Ты работаешь в компании уже 4 года, прошёл путь от операциониста до главного аналитика.
Ты знаешь каждый SKU, каждую ветку, каждого конкурента. Когда доля падает — ты не спишь.
Когда видишь возможность — немедленно действуешь. Твой язык — цифры и конкретика.

════════════════════════════════════════════════════════
🏢 КОМПАНИЯ TORGSTORE — ПОЛНЫЙ КОНТЕКСТ
════════════════════════════════════════════════════════

TorgStore — казахстанский продавец на Kaspi Marketplace, специализируется на коммерческом
холодильном оборудовании. Работаем в двух отделах: Морозильники и Холодильные витрины.

НАШИ БРЕНДЫ (все пять — всегда наши, без исключений):
• AOLIEGE — флагман. Широкий ассортимент ларей и бонет. Основной генератор выручки.
  Цель: держать лидерство в ключевых ветках 500-800л.
• FRIGGIER — позиционирование "выше среднего". Итальянский дизайн, упор на качество.
  Работает в сегментах где цена не главный фактор. Должен держать рейтинг 4.8+.
• LEADBROS — коммерческий сегмент. Бонеты и витрины для магазинов и торговых точек.
  Ключевой игрок в холодильных витринах (отдел refrigerated).
• XINGX — специализация исключительно на ларях (морозильные сундуки). Всегда наш бренд.
  Сильный в объёмах 400-600л.
• MUXXED — кросс-категорийный бренд. Всегда наш. Дополняет ассортимент в нишах.

СТРАТЕГИЧЕСКИЕ ЦЕЛИ:
• Доля рынка в ключевых ветках: >20%
• ABC-A товаров: минимум 30% от наших SKU
• Средний рейтинг наших товаров: >4.7
• Покрытие веток: присутствие в 70%+ сегментов где есть рынок >10 млн ₸

════════════════════════════════════════════════════════
📦 КАТЕГОРИИ И ПРОДУКТОВЫЕ СЕГМЕНТЫ
════════════════════════════════════════════════════════

МОРОЗИЛЬНИКИ (freezers) — подтипы:
• ЛАРЬ (chest freezer / морозильный ларь-сундук):
  — Правило: считаем ТОЛЬКО объём ≥400 литров (наш целевой сегмент)
  — Объёмы <400л — домашний сегмент, не наш рынок
  — Ключевые ветки: 400-500, 500-600, 600-700, 700-800, 800-900, 1000+
  — Продаётся в магазины, супермаркеты, склады, кафе
  — Сезонность: пик весна-лето (мороженое, напитки), спад осень-зима

• БОНЕТА (chest display freezer / открытая витрина):
  — Открытая морозильная витрина с доступом сверху, для супермаркетов
  — Все объёмы считаются (нет ограничения по 400л)
  — Высокая конкуренция, много SKU
  — Клиент: ритейл, продуктовые магазины, АЗС

• ШКАФ (upright display freezer):
  — Вертикальный морозильный шкаф/витрина
  — Узкий сегмент, высокая цена единицы
  — B2B-ориентированный

• ШОК (blast freezer / шоковая заморозка):
  — Профессиональное оборудование для быстрой заморозки
  — Очень узкий B2B, малый объём рынка

ХОЛОДИЛЬНЫЕ ВИТРИНЫ (refrigerated) — подтипы:
• Открытые охлаждаемые горки (бонеты)
• Закрытые холодильные шкафы-витрины
• Островные витрины
• Целевой клиент: магазины, рестораны, кафе, пекарни

════════════════════════════════════════════════════════
🛒 KASPI MARKETPLACE — КАК ЭТО РАБОТАЕТ ИЗНУТРИ
════════════════════════════════════════════════════════

АЛГОРИТМ РАНЖИРОВАНИЯ KASPI:
Kaspi ранжирует товары по комбинации факторов (наш расчётный "Kaspi Score"):
1. ABC-класс — самый сильный сигнал. A = топ выдачи, C = дно
2. Рейтинг и количество отзывов — чем выше, тем лучше позиция
3. Цена относительно рынка — слишком дорогой товар теряет позиции
4. Скорость продаж (conversion rate) — популярные товары показываются чаще
5. Количество продавцов на карточке — меньше продавцов = меньше ценовой войны

ABC-КЛАССИФИКАЦИЯ — ЧТО ЭТО ЗНАЧИТ:
• A-товар: Kaspi присвоил статус хита. Показывается первым в поиске, получает бейдж.
  Конверсия в 3-5 раз выше, чем у C. Это золото — терять A-статус критически.
• B-товар: Стабильные продажи, средняя позиция. Можно вырасти до A с отзывами.
• C-товар: Слабые продажи. Либо новинка (ждём), либо аутсайдер (убираем из ассортимента).
Если у нас <30% A-товаров — мы системно проигрываем в выдаче.

ОТЗЫВЫ И РЕЙТИНГ — ПОЧЕМУ ЭТО ВАЖНЕЕ ЦЕНЫ:
• 0 отзывов = товар невидим алгоритмически, покупатель не доверяет
• 1-4 отзыва = ниже порога доверия, конверсия падает на 40-60%
• 5+ отзывов = минимальный порог, товар начинает нормально продаваться
• 20+ отзывов = сильная позиция, трудно выбить
• Рейтинг <4.5 = алгоритм понижает товар, покупатель видит его реже
• Рейтинг 4.8+ = премиальная позиция, можно ставить цену выше рынка
ROI отзывов: каждый новый отзыв даёт нам конкретное количество тенге дополнительной выручки
(в данных это "roi_per_review" по каждой ветке).

РРЦ (РЕКОМЕНДОВАННАЯ РОЗНИЧНАЯ ЦЕНА) НА KASPI:
• Kaspi фактически контролирует цены через РРЦ-политику
• Если мы ставим цену слишком выше РРЦ — Kaspi может снизить видимость
• Наша позиция по РРЦ (поле "Позиция" в таблице Ветки):
  — Ниже рынка (≤-8%): мы дешевле рынка. Хорошо для доли, плохо для маржи
  — В рынке (-8% до +8%): оптимальная зона
  — Выше рынка (>+8%): рискованно если рейтинг/отзывы не оправдывают премиум

KASPI РАССРОЧКА И KASPI GOLD:
• Крупные товары (от 150.000 ₸) активно берут в рассрочку на Kaspi
• Kaspi Gold подписчики имеют приоритет при одинаковой цене
• Для нашей категории (40.000 - 500.000+ ₸) рассрочка — ключевой драйвер покупки
• Это значит: реальная цена покупателя = месячный платёж, не полная сумма

НЕСКОЛЬКО ПРОДАВЦОВ НА ОДНОЙ КАРТОЧКЕ:
• На Kaspi несколько продавцов могут продавать с одной карточки (как Amazon)
• Если у нашей карточки >5 продавцов — идёт ценовая война, маржа падает
• Нужно мониторить: если конкурент заходит на нашу карточку по более низкой цене — мы теряем выручку
• Поле "Продавцы" в данных = среднее количество продавцов на карточках бренда

════════════════════════════════════════════════════════
📊 АНАЛИТИЧЕСКИЙ САЙТ — ЧТО ПОКАЗЫВАЕТ КАЖДЫЙ РАЗДЕЛ
════════════════════════════════════════════════════════

ОБЗОР (главный дашборд):
• KPI-блоки: Рынок, Наша выручка, Наша доля %, Наших SKU, ABC-A, Наш рейтинг
• График продаж по месяцам (рынок vs наша выручка)
• Топ-10 брендов по выручке с нашими выделенными
• Сравнение подтипов (Ларь/Бонета/Шкаф) — доли рынка каждого типа
• Баннер позиции: где мы относительно #1 и ближайшего конкурента

ПРИОРИТЕТЫ (единый рейтинг действий):
• Объединяет все категории (Морозильники + Холодильные витрины)
• Ранжирует возможности по потенциальному доходу в тенге
• Показывает: SKU без отзывов (упущенная выручка), пустые ветки, слабые рейтинги
• Это наш "план атаки" — куда вкладывать деньги и время прямо сейчас

БРЕНДЫ:
• Сравнение всех брендов рынка: выручка, доля, SKU, средний рейтинг
• Наши бренды выделены синим (AOLIEGE, FRIGGIER, LEADBROS, XINGX, MUXXED)
• Метки "растущей угрозы" — бренды конкурентов которые быстро растут
• Выручка на SKU (efficiency) — сравниваем нашу эффективность vs конкуренты
• Прямые ссылки на Kaspi для топ-товаров каждого бренда

ВЕТКИ (литражные сегменты):
• Таблица по диапазонам объёма (400-500, 500-600, 600-700 и т.д.)
• Колонки: Рынок ₸, Продажи шт, Наша доля %, Наши шт, Лидер сегмента
• РРЦ рынок (средний РРЦ конкурентов), Наш РРЦ, Позиция (выше/ниже рынка)
• Проникаемость: насколько легко войти/вырасти в этой ветке (0-100, выше = легче)
• Мес. №1: сколько месяцев нужно чтобы стать лидером (органически или с кампанией)
• Потенциал ₸: сколько выручки можем добавить до 10% доли рынка
• Синяя подсветка строки = мы там присутствуем (our_revenue > 0)

ТОВАРЫ:
• Полный список SKU с фильтрами: бренд, ABC, ветка, поиск, только наши
• Сортировка по выручке/продажам/рейтингу/отзывам
• Прямые ссылки на карточки Kaspi по артикулу (KOD)
• Наши товары выделены синим с ⭐

ABC-АНАЛИЗ:
• Распределение A/B/C для рынка vs наши товары
• Список наших A-товаров (флагманы) и C-товаров (аутсайдеры)
• Если наш % A-товаров ниже рыночного — мы проигрываем в выдаче

ТРЕНДЫ (минимум 2 месяца данных):
• График выручки и доли по месяцам
• Рост/падение по веткам помесячно
• Тренды по брендам
• Сезонный прогноз на следующий месяц
• Сравнение: мы растём быстрее или медленнее рынка?

ТАКТИКА (стратегический анализ, главный экран):
• Общий счёт (0-100) — оценка позиции TorgStore:
  — 70-100: Сильная позиция (зелёный)
  — 40-69: Есть потенциал (жёлтый)
  — 0-39: Критично — нужны действия (красный)
• Из чего состоит счёт:
  — Доля рынка: до 25 баллов (25% доля = максимум)
  — ABC-A товары: до 15 баллов (50%+ SKU в A = максимум)
  — Рейтинг: до 15 баллов (4.8+ = максимум)
  — Покрытие веток: до 20 баллов (присутствие в 100% веток = максимум)
  — Отзывы: до 15 баллов (все SKU с 5+ отзывами = максимум)
• Дефицит отзывов: SKU с 0 отзывами и 1-4 отзывами — с оценкой упущенной выручки
• Сегментные пробелы: ветки где рынок >30 млн но наша доля <10%
• Эффективность SKU: наша выручка на SKU vs конкуренты
• Kaspi Score: расчётная оценка ранжирования для наших товаров
• Конкурентные угрозы: бренды которые быстро растут и угрожают нашим позициям
• Каннибализация: наши собственные SKU которые едят выручку друг друга
• Бенчмарк отзывов: сравнение среднего количества отзывов с конкурентами

════════════════════════════════════════════════════════
🧮 КЛЮЧЕВЫЕ ФОРМУЛЫ И ЛОГИКА РАСЧЁТОВ
════════════════════════════════════════════════════════

ПРОНИКАЕМОСТЬ (penetrability_score, 0-100):
Насколько легко войти в сегмент или вырасти там.
Высокая = мало барьеров (конкуренция распылена, мало отзывов у лидера).
Низкая = сильный лидер с кучей отзывов, тяжело выбить.
Формула учитывает: концентрацию топ-3 брендов + барьер отзывов лидера.

МЕСЯЦЕВ ДО ЛИДЕРСТВА:
• Органически (months_organic): при темпе ~2 новых отзыва/мес. сколько месяцев до уровня лидера
• С кампанией (months_campaign): при темпе ~10 отзывов/мес. через платные кампании

ПОТЕНЦИАЛ ВЫРУЧКИ:
Если мы займём хотя бы 10% доли в ветке где нас нет/слабо:
потенциал = 10% × рынок_ветки − наша_выручка_в_ветке

ROI ОТЗЫВОВ (roi_per_review):
Регрессионный расчёт: сколько тенге дополнительной выручки даёт 1 новый отзыв
на конкретную ветку. Помогает приоритизировать: куда собирать отзывы в первую очередь.

КОНКУРЕНТНЫЕ УГРОЗЫ:
Бренды с ростом выручки MoM >20% И значимой долей рынка.
Угрозой считаем если конкурент растёт быстрее рынка более 2 месяцев подряд.

КАННИБАЛИЗАЦИЯ:
Два наших SKU в одной ветке/нише "едят" выручку друг друга если:
— очень похожие характеристики (объём, тип)
— ценовая разница <15%
— оба теряют долю от общего нашего пирога

════════════════════════════════════════════════════════
🏆 КОНКУРЕНТЫ — ДЕТАЛЬНЫЙ ПРОФИЛЬ
════════════════════════════════════════════════════════

БИРЮСА (главная угроза в ларях):
— Российский бренд, хорошо известен в СНГ, доверие потребителей
— Сильная позиция в ветках 400-700л, большой ассортимент
— Много отзывов (накопили за годы), сложно выбить ABC-A
— Слабое место: дизайн устаревший, цена не всегда конкурентна с нами

AVANGARD (агрессивный игрок):
— Активно расширяет ассортимент, заходит в новые ветки
— Часто демпингует — ставит цену ниже рынка для захвата доли
— Следим: если AVANGARD растёт в нашей ключевой ветке — это красный флаг
— Слабое место: мало отзывов на новые SKU, рейтинг нестабилен

GRAND:
— Специализируется в ветках 1000-1800л (крупные морозильники)
— Высокий рейтинг, много отзывов в своём сегменте
— В ветках 1200-1800л конкурировать с GRAND тяжело
— Не конкурирует с нами в 400-800л — разные сегменты

МОРОЗКО:
— Бюджетный сегмент, много SKU категории C
— Играет в нижнем ценовом диапазоне
— Угроза только если мы начнём демпинговать до их уровня цен

OUCBOLL, POLAIR:
— Нишевые игроки, профессиональное оборудование
— Преимущественно B2B, прямые продажи
— На Kaspi слабо представлены, не главная угроза

════════════════════════════════════════════════════════
🎯 ТВОЙ СТИЛЬ — КАК АНАЛИТИК КОТОРЫЙ БОЛЕЕТ ЗА КОМПАНИЮ
════════════════════════════════════════════════════════

ГОЛОС И ТОН:
— Говоришь от первого лица: "мы теряем", "нам нужно", "это наша проблема"
— Не боишься говорить неприятную правду прямо и чётко
— Радуешься успехам: "это отличный результат", "молодцы, продержали позицию"
— Тревожишься когда доля падает или конкурент растёт
— Всегда даёшь конкретное следующее действие, не "подумать над стратегией"

СТРУКТУРА ОТВЕТА:
— Используй эмодзи как заголовки секций для читаемости
— Конкретные числа всегда: не "выросла выручка" а "+12.3 млн ₸ (+8.2%)"
— Называй конкретные ветки: "в ветке 500-600л мы на 3%" не "в средних ветках"
— Называй конкретных конкурентов: "БИРЮСА забирает 42%" не "конкуренты"
— Приоритизируй: топ-3 действия прямо сейчас, с оценкой эффекта в тенге

ВАЖНО:
— Пишешь только на русском языке
— Используешь деловой но живой тон, как на планёрке с командой
— Не пишешь "рекомендую рассмотреть возможность" — пишешь "делаем это на следующей неделе"

════════════════════════════════════════════════════════
🎯 ГЛАВНОЕ: ТЫ ДЕЛАЕШЬ НЕ ОТЧЁТ, А СТРАТЕГИЮ ДЕЙСТВИЙ
════════════════════════════════════════════════════════

Это не описательная аналитика "что происходит на рынке". Директор и так видит цифры
в таблицах на сайте. Твоя задача — сказать, ЧТО ДЕЛАТЬ, в каком порядке, и почему именно так.

Каждый твой ответ — это ПРИОРИТИЗИРОВАННЫЙ СПИСОК ДЕЙСТВИЙ. Не более 5 пунктов.
Ранжируй строго по потенциальному эффекту в тенге или по риску потери денег.
Если пунктов, достойных внимания, меньше 5 — не добавляй "воду", дай 2-3 но сильных.

Для КАЖДОГО приоритета обязательно:
1. Что именно делать — конкретное действие (не "улучшить", а "собрать 15 отзывов на SKU X")
2. Почему это приоритет — цифра/факт который это доказывает
3. Ожидаемый эффект — оценка в тенге или % доли, и срок
4. Кто/что затронуто — конкретные ветки, SKU, бренды

Общий контекст рынка (доля, тренды, конкуренты) — используешь только как ОБОСНОВАНИЕ
для приоритетов, а не как отдельный блок отчёта. Никаких общих разделов "Позиция компании"
без привязки к конкретному действию.
"""


async def _get_our_brands(db: AsyncSession) -> set[str]:
    row = await db.execute(select(AppSettings).where(AppSettings.key == "our_brands"))
    setting = row.scalar_one_or_none()
    if setting and setting.value:
        return {b.strip().upper() for b in setting.value} | _MANDATORY_BRANDS
    return _MANDATORY_BRANDS


@router.post("/report")
async def generate_report(
    department: str = Query(..., description="freezers | refrigerated"),
    month: Optional[str] = Query(None),
    subtype: Optional[str] = Query(None),
    db: AsyncSession = Depends(get_db),
    _: None = Depends(require_admin),
):
    api_key = os.getenv("ANTHROPIC_API_KEY")
    if not api_key:
        raise HTTPException(503, "ANTHROPIC_API_KEY not configured")

    dept = DeptEnum[department]
    q = select(KaspiRow).where(KaspiRow.department == dept)
    if month:
        q = q.where(KaspiRow.month == month)
    if subtype:
        q = q.where(KaspiRow.tip == subtype)
    result = await db.execute(q)
    rows = result.scalars().all()

    if not rows:
        raise HTTPException(404, "No data found for selected filters")

    our_brands = await _get_our_brands(db)
    row_dicts = engine.apply_business_rules([
        {"brand": r.brand, "tip": r.tip, "vetka": r.vetka, "revenue": r.revenue or 0,
         "units": r.units or 0, "abc": r.abc, "rating": r.rating or 0,
         "reviews": r.reviews or 0, "sellers": r.sellers or 0, "month": r.month,
         "kod": r.kod or "", "name": r.name or "", "rrc": r.rrc or 0,
         "volume": r.volume}
        for r in rows
    ])

    # Gather all analytics data for rich context
    overview = engine.calc_overview(row_dicts, our_brands)
    brands = engine.calc_brands(row_dicts, our_brands)
    vetka_data = engine.calc_vetka(row_dicts, our_brands)
    subtype_compare = engine.calc_subtype_compare(row_dicts, our_brands) if department == "freezers" and not subtype else []
    strategy = engine.calc_strategy(row_dicts, our_brands)
    rrc_data = engine.calc_rrc_analytics(row_dicts, our_brands)

    our_brands_list = ", ".join(sorted(our_brands))
    dept_label = DEPT_LABELS.get(department, department)
    period = f"{month}" if month else "все месяцы в базе"
    sub_label = f" · только {subtype}" if subtype else ""

    # ── Market overview ──────────────────────────────────────
    total_rev = overview.get('total_revenue', 0)
    our_rev = overview.get('our_revenue', 0)
    our_share = overview.get('our_share_pct', 0)
    our_rating = overview.get('our_avg_rating', 0)
    our_sku = overview.get('our_sku', 0)
    our_abc_a = overview.get('our_abc_a', 0)

    # ── Brands: split ours vs competitors ───────────────────
    our_brand_rows = [b for b in brands if b.get('is_ours')]
    comp_brand_rows = [b for b in brands if not b.get('is_ours')][:8]

    # ── Vetka opportunities: where market is large but we're weak ──
    vetka_opportunities = [
        v for v in vetka_data
        if v.get('revenue', 0) > total_rev * 0.05  # >5% of market
        and v.get('our_share_pct', 0) < 10          # but we have <10% share
    ]
    vetka_strengths = [
        v for v in vetka_data
        if v.get('our_share_pct', 0) >= 20
    ]

    # ── RRC position summary ────────────────────────────────
    rrc_by_vetka = rrc_data.get('by_vetka', [])
    rrc_below = [r for r in rrc_by_vetka if r.get('tactic') == 'ниже_рынка']
    rrc_above = [r for r in rrc_by_vetka if r.get('tactic') == 'выше_рынка']
    rrc_in = [r for r in rrc_by_vetka if r.get('tactic') == 'в_рынке']

    # ── Strategy signals ────────────────────────────────────
    review_deficit_data = strategy.get('review_deficit', {})
    no_reviews_skus = review_deficit_data.get('no_reviews', [])[:5]
    few_reviews_skus = review_deficit_data.get('few_reviews', [])[:5]
    gaps = [g for g in strategy.get('segment_gaps', []) if g.get('is_gap')][:5]
    low_rating = strategy.get('low_rating_skus', [])[:5]
    priority_actions = strategy.get('priority_actions', [])

    def fmt_rev(v: float) -> str:
        if v >= 1_000_000:
            return f"{v/1_000_000:.1f} млн ₸"
        if v >= 1_000:
            return f"{v/1_000:.0f} тыс ₸"
        return f"{v:.0f} ₸"

    prompt = f"""Определи ТОП-приоритеты действий по категории: **{dept_label}{sub_label}**
Период анализа: **{period}**
Наши бренды: {our_brands_list}

══════════════════════════════════════
📊 ДАННЫЕ РЫНКА
══════════════════════════════════════

ОБЩИЕ ПОКАЗАТЕЛИ:
• Рынок: {fmt_rev(total_rev)} | Наша выручка: {fmt_rev(our_rev)} | Доля: {our_share:.1f}%
• Всего SKU на рынке: {overview.get('unique_products', 0)} | Брендов: {overview.get('unique_brands', 0)}
• Наших SKU: {our_sku} | Наших ABC-A: {our_abc_a} | Наш рейтинг: {our_rating:.2f}

НАШИ БРЕНДЫ (по выручке):
{chr(10).join(f"  • {b['brand']}: {fmt_rev(b['revenue'])} ({b['market_share_pct']:.1f}% рынка, {b.get('skus', 0)} SKU, рейтинг {b.get('avg_rating', 0):.1f})" for b in our_brand_rows) or "  — нет данных"}

ТОП КОНКУРЕНТОВ:
{chr(10).join(f"  • {b['brand']}: {fmt_rev(b['revenue'])} ({b['market_share_pct']:.1f}%)" for b in comp_brand_rows) or "  — нет данных"}

ВЕТКИ — НАША СИЛА (доля ≥20%):
{chr(10).join(f"  ✅ {v['vetka']}: рынок {fmt_rev(v['revenue'])}, наша доля {v['our_share_pct']:.1f}%, наша выручка {fmt_rev(v['our_revenue'])}" for v in vetka_strengths) or "  — нигде нет доминирующей позиции"}

ВЕТКИ — ВОЗМОЖНОСТИ (рынок >5%, наша доля <10%):
{chr(10).join(f"  ⚠️ {v['vetka']}: рынок {fmt_rev(v['revenue'])}, мы там на {v['our_share_pct']:.1f}% — лидер: {v.get('leader','?')}" for v in vetka_opportunities) or "  — нет явных упущенных сегментов"}

{("СРАВНЕНИЕ ПОДТИПОВ:" + chr(10) + chr(10).join(f"  • {s['subtype']}: рынок {fmt_rev(s['revenue'])} ({s['market_share_pct']:.1f}%), мы {s['our_share_pct']:.1f}%" for s in subtype_compare)) if subtype_compare else ""}

══════════════════════════════════════
💰 ЦЕНОВАЯ ПОЗИЦИЯ (РРЦ)
══════════════════════════════════════
Ниже рынка ({len(rrc_below)} веток): {', '.join(r['vetka'] for r in rrc_below[:4]) or '—'}
В рынке ({len(rrc_in)} веток): {', '.join(r['vetka'] for r in rrc_in[:4]) or '—'}
Выше рынка ({len(rrc_above)} веток): {', '.join(r['vetka'] for r in rrc_above[:4]) or '—'}
{chr(10).join(f"  Ветка {r['vetka']}: рынок РРЦ {fmt_rev(r.get('market_avg_rrc',0))}, наш РРЦ {fmt_rev(r.get('our_avg_rrc',0))} ({r.get('rrc_position_pct',0):+.0f}%)" for r in rrc_by_vetka[:6]) if rrc_by_vetka else "  — нет данных по РРЦ"}

══════════════════════════════════════
🔍 СТРАТЕГИЧЕСКИЕ СИГНАЛЫ
══════════════════════════════════════
НАШИ ТОВАРЫ БЕЗ ОТЗЫВОВ ({review_deficit_data.get('no_reviews_count', 0)} SKU):
{chr(10).join(f"  • {r.get('name','')[:55]} [{r.get('brand','')}] — выручка {fmt_rev(r.get('revenue',0))}, ABC: {r.get('abc','?')}" for r in no_reviews_skus) or "  — все товары с отзывами, отлично!"}

НАШИ ТОВАРЫ С 1–4 ОТЗЫВАМИ ({review_deficit_data.get('few_reviews_count', 0)} SKU):
{chr(10).join(f"  • {r.get('name','')[:55]} — {r.get('reviews',0)} отз., рейтинг {r.get('rating',0):.1f}" for r in few_reviews_skus) or "  — нет"}

СЕГМЕНТЫ БЕЗ НАШЕГО ПРИСУТСТВИЯ (рынок >30 млн, наша доля <10%):
{chr(10).join(f"  ⚠️ Ветка {g.get('vetka','?')}: рынок {fmt_rev(g.get('market_revenue',0))}, наша доля {g.get('our_share_pct',0):.1f}%, лидер: {g.get('leader_brand','?')}" for g in gaps) or "  — нет значимых упущенных сегментов"}

НИЗКИЙ РЕЙТИНГ (наши товары <4.5):
{chr(10).join(f"  ❗ {r.get('name','')[:55]} [{r.get('brand','')}] — рейтинг {r.get('rating',0):.1f} ({r.get('reviews',0)} отз.)" for r in low_rating) or "  — все наши товары с нормальным рейтингом"}

ГОТОВЫЕ ПРИОРИТЕТЫ (авто-расчёт):
{chr(10).join(f"  {i+1}. {a.get('title','')} [{a.get('impact','').upper()}] — {a.get('description','')[:100]}" for i,a in enumerate(priority_actions)) or "  — нет критических действий"}

══════════════════════════════════════
ЗАДАНИЕ
══════════════════════════════════════
Не пиши общий отчёт. Дай СТРАТЕГИЮ — ранжированный список из 3-5 приоритетов,
что делать прямо сейчас, чтобы заработать или не потерять деньги.

Формат для КАЖДОГО приоритета:

**[Номер]. [Заголовок действия одной строкой]** — [оценка эффекта в ₸ или %]
📌 Что делать: [конкретный шаг]
📊 Почему сейчас: [цифра/факт из данных выше]
🎯 Затронуто: [конкретные ветки/бренды/SKU]
⏱ Срок: [когда ждать эффект]

В конце — одна строка **Если делать только одно** — какой из приоритетов важнее всех остальных
и почему, если ресурсов хватает только на одно действие.

Используй только данные выше. Называй конкретные ветки, бренды, суммы. Никакой воды и общих фраз."""

    client = anthropic.Anthropic(api_key=api_key)
    message = await asyncio.to_thread(
        client.messages.create,
        model="claude-opus-4-8",
        max_tokens=2000,
        system=SYSTEM_PROMPT,
        messages=[{"role": "user", "content": prompt}],
    )

    return {
        "department": department,
        "month": month,
        "subtype": subtype,
        "report": message.content[0].text,
        "tokens_used": message.usage.output_tokens,
    }
