# Правило 3 секунд при выгрузке из CRM — инструкция для агента

Самодостаточный файл, не зависит от TorgStore. Можно копировать в любой другой проект как CLAUDE.md / project instructions или просто как загруженный документ в контекст.

## Зачем это правило

Массовая выгрузка данных из CRM в цикле без паузы (несколько запросов в секунду) визуально неотличима от атаки на сервер. В одном из проектов ровно это и произошло: агент дёргал CRM без пауз при массовом импорте, сотрудники заказчика расценили это как атаку и заблокировали доступ. Восстановление доступа обошлось дороже, чем медленная, но безопасная выгрузка с самого начала.

Отсюда правило: **скорость выгрузки не важна, важно не попасть под блокировку.**

## Само правило

Между ЛЮБЫМИ двумя запросами к домену CRM (любой эндпоинт — списки, детали лида/сделки, аналитика, фильтры и т.д.) должно проходить **минимум 3 секунды**. Без исключений, независимо от того, сколько времени прошло с момента установки этого правила и в какой сессии/разговоре находится агент.

Практически это значит:

- Любой bash/python-скрипт, который в цикле обращается к CRM (импорт по сущностям × периодам и т.п.), обязан делать `time.sleep(3)` (или больше) между итерациями — не 0.5, не 1 секунду, а минимум 3.
- Любая последовательность действий в браузере (клики по фильтрам, смена периода, наведение, вызывающее AJAX-запрос на каждое действие) тоже должна быть разнесена минимум на 3 секунды между сетевыми запросами к CRM, а не только между UI-действиями.
- Если нужно выгрузить много данных (по многим сущностям/периодам) — это нормально, что это займёт минуты, а не секунды.
- Перед любым новым скриптом импорта — явно перепроверить, что пауза заложена в код, а не полагаться на память/намерение.
- Если неясно, считается ли конкретное действие «запросом к CRM» — по умолчанию трактовать в пользу осторожности и добавлять паузу.

## Технический guard — не полагаться только на память агента

Текстовое правило выше — это то, что агент обязан ПОМНИТЬ. Чтобы не зависеть от памяти (в т.ч. в новой сессии, у другого агента, после сжатия контекста), используется JS-снипет, который технически ГАРАНТИРУЕТ ≥3с между запросами к домену CRM — перехватывает `fetch` и `XMLHttpRequest` на странице CRM и придерживает любой запрос к этому домену, пока не пройдёт 3с с прошлого. Состояние хранится в `localStorage`, поэтому переживает reload вкладки (но не переживает переход на новый URL внутри домена CRM — там JS страницы полностью пересоздаётся).

```js
// CRM rate-limit guard — переживает reload (localStorage, а не память вкладки).
// Перехватывает fetch И XMLHttpRequest — то есть работает и для ручных fetch,
// и для запросов, которые сам UI шлёт при клике/наведении/смене фильтра.
//
(function(){
  const CRM_DOMAIN = 'zymyran.com';
  const KEY='__crm_rl_last_ts', MIN_GAP=3000;
  async function throttle(){
    const last=Number(localStorage.getItem(KEY)||0), now=Date.now();
    const wait=MIN_GAP-(now-last);
    if(wait>0) await new Promise(r=>setTimeout(r,wait));
    localStorage.setItem(KEY,String(Date.now()));
  }
  window.__crmThrottle = throttle;
  const origFetch = window.fetch;
  window.fetch = async function(input, init){
    const url = typeof input==='string' ? input : (input && input.url) || '';
    if(url.includes(CRM_DOMAIN)) await throttle();
    return origFetch.call(this, input, init);
  };
  const OrigXHR = window.XMLHttpRequest;
  function PatchedXHR(){
    const xhr = new OrigXHR();
    const origOpen = xhr.open;
    xhr.open = function(method, url, ...rest){
      this.__isCrm = String(url).includes(CRM_DOMAIN);
      return origOpen.call(this, method, url, ...rest);
    };
    const origSend = xhr.send;
    xhr.send = function(...args){
      if(this.__isCrm){ throttle().then(()=>origSend.apply(this,args)); }
      else { origSend.apply(this,args); }
    };
    return xhr;
  }
  window.XMLHttpRequest = PatchedXHR;
  console.log('%c[CRM rate-limit guard armed] минимум 3с между запросами к CRM, живёт до reload/навигации', 'color:#0a0;font-weight:bold');
})();
```

## Обязательный порядок действий при работе с вкладкой CRM в браузере

Это касается и работы через консоль вручную, и через браузерную автоматизацию (например, Claude in Chrome / `javascript_tool`):

1. Открыть/перейти на вкладку CRM.
2. ПЕРВЫМ ДЕЙСТВИЕМ выполнить guard-снипет выше (например через `javascript_exec`).
3. Только после этого — любые клики, навигация внутри домена CRM, ручные `fetch`-запросы к CRM.
4. Если случился reload или переход на новый URL внутри домена CRM — guard слетает (JS-состояние вкладки сбрасывается), значит перед следующим действием вставить его заново (шаг 2).

Guard не отменяет `time.sleep(3)` в python/bash-скриптах, которые обращаются к CRM напрямую (не через браузер) — для них технического перехвата нет, там пауза зашивается в сам скрипт.

## Быстрая проверка, что guard стоит и не сработает случайно рано

```js
JSON.stringify({
  guardArmed: !!window.__crmThrottle,
  lastRequestTs: localStorage.getItem('__crm_rl_last_ts'),
  now: Date.now()
});
```

## CRM-домен TorgStore

`zymyran.com` — уже зашит в guard-снипет выше.

---

# ДОПОЛНЕНИЯ ОТ 12.08.2026

## ⏰ ОКНО 09:30–11:00 ЗАНЯТО — не подходить к CRM (Дамир, 12.08.2026)

Ежедневно с **09:30** идёт выгрузка другого проекта (TorgStore Analytics,
задача `torgstore-data-freshness-check`) — она длится **чуть больше часа**.

**К CRM по проекту Kaspi не подходить раньше 11:00.** Ни задачей, ни руками в
чате, ни «одним лёгким запросом посмотреть».

### Почему это важнее, чем кажется

Два независимых процесса, каждый честно выдерживающий 3 секунды, при
чередовании дают серверу эффективный интервал **1.5 секунды**. Формально
правило соблюдают оба — фактически вместе они выглядят как перебор.

Скоординироваться они не могут: guard хранит отметку в `localStorage` браузера,
а импорт другого проекта работает своим контекстом. Общего счётчика нет.
**Единственная защита между независимыми процессами — временное разделение.**

### Практически

| Время | Кто работает с CRM |
|---|---|
| 09:30 – ~10:45 | Ежедневный импорт TorgStore Analytics. Не мешать |
| до 11:00 | Буфер на переработку |
| после 11:00 | Окно для задач и ручной выгрузки проекта Kaspi |

Понедельничная задача `crm-sync-ponedelnik` поэтому перенесена с 10:00 на
**11:00**. Ручная выгрузка в чате — тоже только после 11:00.

Перед началом любой работы с CRM спросить себя: **сейчас позже 11:00?**
Если раньше — подождать, а не «быстренько, пока никто не заметил».



## Иерархия приоритетов при работе с CRM (добавлено 12.08.2026)

1. Не нарушить правило 3 секунд
2. Не залить мусор на прод
3. Собрать данные

**Данные не собраны, правило соблюдено → УСПЕШНЫЙ прогон.
Данные собраны, правило нарушено → ПРОВАЛ**, качество данных не важно.
Нужно 40 минут вместо 5 — тратим 40. Скорость не имеет значения вообще.

> ⛔ **Автоматика проекта Kaspi в CRM не ходит.** Решение Дамира 12.08.2026:
> задача `crm-sync-ponedelnik` не обращается к `zymyran.com`, выгрузка для
> закупа делается только в живом чате. Причина: соблюдение невозможно
> проконтролировать в реальном времени — сетевые запросы не видны на экране,
> а агент внутри автоматического прогона проверяет сам себя.
> **Исключение:** задача `torgstore-data-freshness-check` (проект TorgStore
> Analytics) ходит в CRM ежедневно по своему runbook §0 с v3-паттерном
> (`sleep(3000)` первой строкой, последовательный `for...of`, запрет
> `Promise.all`, аудит-лог, самопроверка `gaps.every(g=>g>=2.9)` с `throw`).
> Это отдельная система со своей механической защитой — не путать.

## ЗАПРЕЩЕНО в браузере

`browser_batch` и любые батч-инструменты на вкладке CRM · циклы в
`javascript_tool` по нескольким URL · параллельные вкладки CRM · немедленный
повтор после неудачи · ускорение в любой форме.

## Аварийная остановка

Немедленно прекратить все обращения, ничего не повторять, сообщить Дамиру —
при HTTP 429/403/503, капче, сообщении о превышении лимита, зависших ответах,
разлогине посреди работы.


# Шаг 0 · ВООРУЖИТЬ ЖУРНАЛ СЕТИ — самое первое действие, до открытия CRM

```
mcp__claude-in-chrome__read_network_requests {tabId, urlPattern:"zymyran", limit:100}
```

**Журнал начинает писать только с момента первого вызова.** Задним числом
измерить нельзя. Поэтому вызвать его ДО того, как открыть или перезагрузить
вкладку CRM — иначе загрузка страницы пройдёт мимо измерения.

## Зачем, если есть guard

Guard перехватывает `fetch` и `XHR`, но существует только ПОСЛЕ установки.
Запросы, летящие в момент загрузки страницы, он не видит и не считает — то
есть его счётчик систематически ЗАНИЖАЕТ реальный трафик. Журнал сети видит всё.

## Что показало измерение 12.08.2026

Одна загрузка `/service/analytics/products/sales` = **14 обращений к домену**:

| Тип | Штук | Что это |
|---|---|---|
| Документ страницы | 1 | реальный запрос к приложению |
| CSS / JS бандлы | 7 | plugins.bundle, style.bundle, datatables, widgets |
| Шрифты | 2 | fontawesome, keenicons |
| Картинки | 4 | логотипы SVG, calendar.png |

**Вывод, который меняет модель риска:** 13 из 14 — статика. Ровно столько же
генерирует обычный человек, открывший ту же страницу. Подозрительным выглядит
не общее число HTTP-обращений, а **повторяющиеся запросы ДАННЫХ в цикле** —
именно так выглядела блокировка в прошлый раз.

**Практическое следствие:** риск пропорционален числу НАВИГАЦИЙ, а не действий.
Каждая навигация = пачка статики. Отсюда правило «читать DOM вместо кликов» —
оно не про скорость, а про безопасность. Понедельничный маршрут укладывается в
2 загрузки страниц.

## Что считать при отчёте

Разделять два числа, не смешивать:
* **запросы данных** (документ + XHR/fetch) — то, что guard контролирует и по
  чему меряется соблюдение правила 3 секунд;
* **статика** (css/js/шрифты/картинки) — фон загрузки страниц, к правилу
  отношения не имеет, но показывает, сколько раз мы дёрнули навигацию.

Фильтр для разделения: статикой считать URL, содержащие `/assets/`, либо
кончающиеся на `.css .js .woff2 .ttf .svg .png .jpg .ico`.

---
